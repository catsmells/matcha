# __DISPLAY_NAME__'s site

Generated by [matcha](https://github.com/catsmells/matcha).

## Deploy

Upload everything in this folder to your web host's public directory
(usually `public_html` on cPanel, or `/var/www/html` on a VPS).

## Edit Content

- **Bio** — search for `commands.myself` in `index.html`
- **Contact Information** — search for `commands.contact` in `index.html`
- **Blog Posts** — drop `<slug>.html` files in `blog/` and add a line to `blog/posts.json`
- **Projects** — same pattern in `projects/`
- **Webring** — edit `webring.json`
- **Themes** — search for `THEMES` in `index.html`

Everything is plain HTML/CSS/JS. Open `index.html` in your editor and have at it.

## Regenerate

If you want to rerun the wizard with different options, just run
`matcha` again and choose a new output directory.

## License

Your site's content is yours. The shell engine is MIT-licensed. Credit [me](https://drcat.fun/) if you're feeling generous - I always appreciate the traffic.
